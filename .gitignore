# Runtime state (generated per project)
.ralph/

# Optional local control directory (if configured under repo)
.ralph-control/
telegram.env
telegram.offset

# Local fleet state variants
fleet/projects.local.json
fleet/projects.json

# Build and coverage artifacts
/ralphctl
bin/
dist/
coverage.out
coverage.txt
*.coverprofile

# OS artifacts
.DS_Store
